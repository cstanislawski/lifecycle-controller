apiVersion: lifecycle.cezary.dev/v1alpha1
kind: Lifecycle
metadata:
  labels:
    app.kubernetes.io/name: lifecycle-controller
    app.kubernetes.io/managed-by: kustomize
  name: lifecycle-sample
spec:
  # TODO(user): Add fields here
